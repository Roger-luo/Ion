name = "{{{ project.name }}}"
uuid = "{{{ project.uuid }}}"
authors = [{{#each project.authors}} "{{{ this.firstname }}} {{{ this.lastname }}} <{{{ this.email }}}>", {{/each}} "contributors"]
version = "{{{ project.version }}}"

[deps]

[compat]
julia = "{{{ julia.compat }}}"
