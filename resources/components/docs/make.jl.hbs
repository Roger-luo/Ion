using Documenter
using {{ package }}
using DocThemeIndigo

indigo = DocThemeIndigo.install(Configurations)

makedocs(;
    modules = [{{ package }}],
    format = Documenter.HTML(
        prettyurls = !("local" in ARGS),
        canonical="https://{{ user }}.github.io/{{ package }}.jl",
        assets=String[indigo],
    ),
    pages = [
        "Home" => "index.md",
    ],
    repo = "https://github.com/{{ user }}/{{ package }}.jl",
    sitename = "{{ package }}.jl",
)

deploydocs(; repo = "github.com/{{ user }}/{{ package }}.jl")
